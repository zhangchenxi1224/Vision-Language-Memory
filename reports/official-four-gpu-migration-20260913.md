# Four-H200 migration of the fixed warm-start experiment

The user supplied `vlm-r11-trust-h200x4-20260907-r3`. All four H200s were actually idle, with 143771 MiB each, and the image is ngc-pytorch:25.02-cuda12.8.0-py3. The existing shared environment and model paths are available.

The old single-GPU run `d9a1a11-transition-warm2880-seed20260914` received SIGTERM during its initial baseline. Its worker and parent exited; the authoritative training terminal reports paused, optimizer_steps=0, checkpoint=null. Partial baseline evidence stays in that run. No new optimizer state or weight update exists to migrate. The sealed 2880-step parent package remains the initialization for the replacement run.

`run_transition_four_gpu.py` records a migration amendment before execution. The original warm-start plan, 45 conditions, three teachers, seed20260914, fresh AdamW, additional2880 updates and11520 draws, four development noise seeds, and fresh independent confirmation cases remain unchanged. No validation examples enter training. The new implementation commit is recorded explicitly; this is not an exact continuation of the old serial implementation.

Each rank receives one of the same four globally indexed microbatches per optimizer update. Loss is divided by four, gradients are SUM-reduced in fixed32MiB buckets using NCCL Ring/Simple, then globally clipped at1 before each replica's AdamW update. This preserves the objective and global batch; collective summation can change FP32 rounding. The upstream U-Net and native inference pipeline are not wrapped or rewritten. Evaluation retains native batch-one inference, shards complete condition groups, verifies shard hashes and the full cell matrix, and merges records into the original order. Rank zero owns output locks, logs, metrics and checkpoints. Checkpoints include each rank's RNG state, the fixed world size and numerical protocol. Parameter hashes must agree across all ranks at initialization, recovery, first and final update.

Before teacher checks, baseline and optimization, the actual full U-Net compares the first four draws in serial and parallel. Draw records and per-draw losses must match; global gradient relative L2 and normalized maximum error must be <=2e-6. The preflight makes no optimizer update and restores RNG. Its numerical report is saved in the run. A failed preflight blocks training and retains evidence.

Local verification: actual four-process Gloo gradients, clipped AdamW and bitwise checkpoint recovery passed. The broader relevant test invocation produced66 passes and one environment failure: local D:/st_python lacks installed diffusers metadata required by an existing manifest test. That test must be rerun in the real installed training environment. These tests do not establish NCCL or full-model gradient parity; the real run enforces that separately.

The previous endpoint remains a functional failure: development880/900, independent gray-initial writes360/360, actual RGB chains430/480 and11/16 completely correct chains. Five clear operations preserve a previous state and their following no-ops preserve that wrong state. The sealed package's actual six-write/thirty-read replay parity passed, which establishes engineering equivalence only. A fresh four-GPU run still requires its fixed endpoint and independent functional validation.

The parallel historical oracle readback has also finished its720 records. Both FP32 decoded and RGB-quantized forms have312/320 correct matched reads, with57/64 members passing all five prompts in both forms. All80 blank controls fail. These are direct oracle compatibility results, not shared Writer performance; full evidence collection is separate.
