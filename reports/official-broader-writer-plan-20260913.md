# Expanded conditions for the next shared Writer

The completed four-H200 warm run passed its full development matrix but failed the newly worded independent tests. Its original-expression chain reads were240/240; new expressions accounted for70 chain errors. The next experiment keeps official FM and native inference unchanged and expands the condition-text data.

All45 original source/operation/wording groups are preserved. Each of the15 transitions receives six more expressions: the two already observed warm-confirmation formulations and four additional training formulations. Source images, source latents and target latents are unchanged. The no-op expression is identical across ambient/jazz/clear source states and contains no answer; the model must use the source condition. With all16 newly qualified historical questions, the complete bank has151 conditions and17 semantic questions. The16 historical conditions remain complete ordered event-prefix writes from gray; their initial states and operation order are retained when constructing new validation wording.

The five-query teacher run must actually seal all16 targets before any shared-Writer update. The merger binds its exact918e7d2 source and plan. Every target must pass allfive known queries in FP32 and RGB form. These are training-seen teacher queries, not untouched holdouts.

The fixed parameter initialization is the actual900/900 development endpoint's independently replayed package, manifest SHA256 `d1536d754dc3770f3e715b1c0bfaa3278ff61e68b7bad70dce385b3b38f8a2d7`. Its independent functional failures are retained explicitly. The new experiment uses seed20260915, fresh AdamW at5e-5, the existing full-U-Net scope and four synchronous replicas with global batch4. A complete new two-noise baseline precedes optimization. The fixed endpoint is4832 updates and19328 draws, exactly128 per condition. This changes exposure per expression while increasing total data diversity; it is not an equal-exposure ablation. Each full development phase has1510 matched and3020 total raw records. Native FP32 Base inference stays28 steps, CFG1, pure Gaussian initialization and the upstream scheduler. Source remains exclusively a condition.

Before optimization, the driver writes the complete training plan and new validation cases. Continuous music-state validation keeps the full72 single-write images and16 six-write chains, with a new pair of event expressions for each operation. The now-observed old test expressions are training/regression data. Broader full-prefix validation fixes all16 targets, original plus two semantic-preserving rewordings, four new noise seeds each,192 generated images and960 raw matched reads. It preserves every original event in order and checks the parsed final value against the historical label. No question or gold field enters the Writer condition. Exact reader questions and strict gold-token plus immediateEOS scoring remain fixed.

The driver refuses occupied GPUs, failed qualification, changed parent identities, duplicate output and an insufficient finite time budget. It does not restart failed jobs or select a favorable intermediate endpoint. Actual model performance, later independent validation and package replay remain required; this plan does not establish usability.

## Actual dispatch

The five-query target run completed all16 targets and160/160 matched reads. Its complete seal is `e52d4777e99ea6f2dbf8614ce0b0acd2afc2e305382adb2fd781bd596179688d`; the refined bank is `76ecd98f2bea422b756e7691425973ec9694f44fabca5905731c963a0f38b7dd`. These inputs were bound before the new experiment was dispatched.

Training source84cdfdb58ace96954243de5caf427948717c9abf actually assembled and loaded all151 conditions on shared storage, producing bank `c27cd65dab809deabb5f2cb08891517d3590244651d08a8c6763c84fea901592`. The complete experiment plan was sealed as `c4a6986edf1330e27af5b91e5105ad6e3806040f01d562f791edbd392bd16834`, before four CUDA workers started. The real full-U-Net serial/parallel gradient check passed with relativeL2 error4.2115734627840765e-08; allfour initial parameter hashes exactly match the sealed warm parent. The workers are measuring the full new baseline, with720/3020 raw records observed at11:55 and no optimizer record yet. The fixed training is live, not completed.

Downloaded input metadata, all151 reconstructed condition/teacher records, every registered case and the actual four-rank proofs were reverified locally. Raw refinement trajectories and large target tensors remain on shared storage; their local numerical verification is not claimed. New151-group endpoint collection and independent validation execution still need to be completed while this fixed training runs.
