# 固定 64 条目标图的 FM 预算对照

2026-09-24，启动前登记。A 教师 T1 自然回答正确 40/64，而 write2048 两条学生链
分别只有 4/64；教师正确项分别仅保留 1/40、2/40。B 教师和学生都已有明显自然回答不足。
因此保持问题之外，还要检验共享 Writer 是否仅因当前 FM 拟合预算不足而未保留目标图能力。
本轮不保证更长训练有效，也不把损失下降直接视为视觉记忆成功。

## 唯一训练变量是 FM 总步数

- 固定原 64 条 pilot、A/B 各自原 288 步目标 latent，逐项核对哈希，不重做教师。
- 两组都从原 `4fbc857` 权重重新训练到 **8,192 步**，有效 batch4、AdamW 和学习率不变。
  使用现有冻结 `8cd5785` Writer 的 write 阶段，不修改 FM、条件编码或采样实现。
- 保留第 **2,048 步**固定快照，检查它与原 write2048 权重和训练轨迹的一致性。
  重新执行前 2,048 步是为了只改变总预算，不另引入优化器重置或抽样续接差异。
- 每组总抽取 32,768 次，64 偏好各 512 次；所有样本保留，不根据教师/学生成败筛选。
  8,192 为预先固定终点，不能按 OOD、开发集或中途损失挑选其他步数。

## 读取与判断

固定终点后从灰图生成原 64 pilot 和未训练 dev90 的两条初写 PNG。
使用相同 T1 原题、官方 MCQ 内容与 parser、自然回答、匹配/错配图和既有共享灰图/文本参照。
MCQ 先返回，自然回答继续用相同替代 judge；同一冻结 PNG 的两类成绩分别报告。
报告教师到学生配对、两条链均正确率、截断和未评分项。
如果第 2,048 步快照不与原模型逐参数一致，披露差异，不把原输出冒充新快照输出。

本轮聚焦单次写入的蒸馏充分性，不把它当作保持训练、L1 覆盖或 L2 递归证据。
原保持、仅初写源图的保持对照、730 条覆盖实验继续。
等待本任务 Reader 实例对应卡的当前保持训练完成后启动；复用现有两卡，不申请额外资源，
不触碰禁用实例。它可与随后 RGB 生成共享显存，持续检查实际进程和资源。


## 已部署等待任务

北京时间 09:12，在本任务 Reader 实例启动 A/B 等待进程 3683311/3683315；
已经通过实际 `ps` 确认存活，尚未开始 FM8192 优化。
执行 Writer 固定 `8cd5785`，调度脚本登记提交 `457fd38`。
启动前已核对原 4f 实际权重 SHA256，以及 A/B 各 64 个目标 latent 与原 write2048 清单完全一致。
完成对应保持训练后自动训练、生成 pilot/dev 初写图并读回。
[完整部署参数与目标哈希](evidence/fixed64-fm8192-launch.json)。
