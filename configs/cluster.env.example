# Copy values into the job environment; do not commit credentials.
export VLM_MODEL_ROOT=/shared/path/vision-memory-models
export HF_HOME=/shared/path/huggingface-cache

# Fill this only after inspecting the target node and installing a CUDA-matched PyTorch build.
export VLM_EXPECTED_TORCH=REPLACE_WITH_EXACT_TORCH_VERSION
