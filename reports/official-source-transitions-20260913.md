# Source-conditioned memory transition preparation

Registered while c90896c shared three-state training is still running, before its endpoint is observed. The initial gray-source experiments cannot identify a retention rule: their source never varies. In particular, an event-only lookup could pass all three trained state events while ignoring the previous memory. This limits claims of a usable memory updater even if the current confirmation passes.

Add explicit sealed_rgb_1024 sources to the Base runtime. Read exact bound RGB PNG bytes without implicit resize/color conversion, encode them with the official VAE, and require bitwise equality to the bank's bound source latent. The same PIL image enters the raw-event condition encoder and native inference; the native sampler checks its actual source encoding. Source is still conditioning only: the official target/noise FM bridge is unchanged. The historical gray branch keeps its explicit float0.5-versus-PIL128 binding. Blank controls remain the gray image for every source; an incoming memory is never relabeled as a blank control. Verify source file hashes again at completion.

Prepare three incoming source images by decoding the fixed ambient/jazz/clear oracle targets, clamping and nearest-rounding to uint8 RGB PNG. No text/answer rendering, new latent optimization, target averaging, teacher selection, or Writer checkpoint is used. Each quantized source must pass all five real Reader prompts with immediate EOS before any transition bank is released. A failed source prevents release; keep the failure and do not choose another endpoint.

The fixed bank has15 conditional groups: gray→each of three target states; each of three incoming states→each of three target states; and three no-op transitions. All three no-ops use exactly the same delivery/keep-preference event, but require different answers according to the source image. The15 teacher records reuse exactly three unchanged targets and remain one semantic question, not15 independent questions or targets. Copy the original snapshot bindings and record parent SHA5165c059a0a4c0760cd4b0df1663c642132e52bf55a13031cd99207fd200bb86, image/source tensor hashes, incoming-state readbacks, and explicit reused-teacher provenance.

Before release, load all15 contexts using the actual new runtime. Run the original native28-step pipeline once for each of the three no-op/source pairs with fixed namespace official-source-image-parity-noise-v1. It must encode exactly the source latent already used by training and produce three distinct image-aware condition hashes for the same event. Save every trajectory, but do not score these pretrained Writer outputs as task success. All model weights remain frozen and are checked for gradients/version changes. These checks establish input/provenance parity and readable incoming states; actual learned transitions and chains remain unverified.

Use a separate new single-H200 instance dl-align-sources-h200x1-20260913 with the same NGC25.02/CUDA12.8 environment and120-minute platform limit. This preparation is independent of the active shared Writer; its code/data/checkpoints remain immutable. Select the next training or diagnosis after the current endpoint evidence. This document does not claim a transition model has been trained or passed.
