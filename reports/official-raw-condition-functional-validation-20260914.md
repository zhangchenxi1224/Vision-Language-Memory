# Raw训练条件推理的完整功能对照（执行前固定）

本对照冻结bb34092的最终4832步权重，继续检验“训练使用raw事件、推理使用native diptych条件”的实际影响。它是显式的训练条件一致推理实验，不把raw包装替代称为官方Base默认推理，也不替代正在进行的03f8467原生条件训练。本文编写时完整raw开发读取尚未结束，未按部分结果挑选验证项目。

固定父checkpoint SHA256为737735c4d7d3483b38be2f88d8c48fbe3050b40c8f90c0d49b21e30336455616，raw开发对照为1f86d56全151条件、302图、3020原始回答；要求该对照完整结束并通过独立CPU的604份新旧PT和6040条原始记录核验，方可开始本套功能验证。无论完整开发分数是否全通过，以下全部案例都运行，失败保留为诊断结果。不得按成功案例选子集。

推理仅使用本次事件和当前1024×1024 RGB图像，每次写入重新执行官方LoRA示例的单条raw条件编码，再把embedding与mask复制到native三分支。保留官方source分支、纯高斯起点、28步完整时间表、CFG=1、image CFG=1和FP32。连续记忆只保留上一次生成的真实uint8 RGB，下一次通过官方VAE重新编码；无操作事件也实际采样，Reader查询不修改图像，标签/答案/开发bank不进入Writer条件。当前动态编码适配器须与原缓存raw条件适配器保持相同计算；已完成单元层面轨迹等价与恢复验证，尚待实际GPU功能证据。

全部使用bb父计划原有案例和种子，明确属于已观察案例的配对诊断：

| 验证 | 原始回答行数 | 匹配回答数 | 生成图数 |
| --- | ---: | ---: | ---: |
| 单次写入与表达变化 | 390 | 360 | 72 |
| 16条六步RGB连续链 | 480 | 480 | 96 |
| 历史前缀lane0 | 560 | 480 | 96 |
| 历史前缀lane1 | 560 | 480 | 96 |

共1990条原始记录、1800条匹配回答、360张生成图，固定负对照及原查询全部保留。每张生成图评估五个问题表达，严格greedy32、原gold token IDs+立即EOS151645；不归一化同义回答、不截断补救、不改变分母。链须16/16全部六步正确。完成后CPU逐份检查PT/PNG/真实Gaussian/全部29个有限FP32状态，下载完整原始记录、PNG和seal后本地重算。PT保留在共享盘并披露。

随后导出不含teacher bank、优化器或问答表的参数包。raw策略采用新的v2 schema并显式记录inference_condition=training_raw，旧v1加载器必须拒绝该包，防止静默切回native条件。实际CLI读取包内策略，用固定第一条链的六次write和三十次read重放，逐一对比PNG与原始生成token。工程parity与功能正确率分别报告，均不能用来掩盖对方失败。

使用现有新四卡dl-official-exp-h200x4-20260914，在9e27050原生条件完整suite结束且该实例GPU实际空闲后接续，不停止或修改03/9e/1f86运行中的源码和进程。为本验证创建另一个干净固定commit的源码checkout。截止09-14 09:50 Asia/Shanghai（1789350600），GPU仍忙则等待；不足一小时拒绝启动，依赖失败或超时保留证据并停止后续阶段。原有实例保留。

可用版本验收仍需完整功能证据；当前此计划、单元测试和已注册流程都不构成模型已可用的结论。即使本套全通过，也只覆盖已见语义问题与已固定案例，不能宣称未见实体或任意多事实记忆泛化。

执行记录（09-14 05:58）：35741bd 首次驱动在依赖完成后，因从仓库外绝对路径启动导致 `No module named 'scripts'`，在GPU工作开始前失败。5ef8aa8 补充入口根目录导入路径，并将仅Linux使用的fcntl导入移至main；隔离Python、外部cwd的实际回归测试通过。新干净源码 `dreamlite-raw-functional-validation-r2-20260914` 已在同一四卡实例启动，输出前缀 `5ef8aa8-raw-condition-`，保留原失败现场。该修复不改变本计划的模型、条件、案例、种子、评分或预算。05:55实际CUDA进程2548454–2548457均在运行。
