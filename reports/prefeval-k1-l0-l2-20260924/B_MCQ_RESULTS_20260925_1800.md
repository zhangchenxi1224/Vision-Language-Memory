# B MCQ 首批完整节点：730教师与C64保持

2026-09-25 18:00 CST。实验主线继续运行。本报告只汇总已经完整结束的读取矩阵；
I64模型训练完成不等于其保持评测完成，B730共享Writer仍在训练。

## 1. 730教师：目标图的受限MCQ可读性已在全训练集确认

每条偏好独立优化同一个B目标latent，原288步预算、3训练问法，Reader冻结。
本次读取包含全部730条，每个条件730次，无筛选，无缺行，解析失败和截断均为0。

| T1条件 | 正确/总数 | 准确率 |
|---|---:|---:|
| 对应教师PNG | 726/730 | 99.45% |
| 同主题错配教师PNG | 312/730 | 42.74% |
| 灰图 | 301/730 | 41.23% |
| 原官方SFT初始交换全文 | 648/730 | 88.77% |

对应图相对错配图的配对结果：414次修复、0次退步、312次都正确、4次都错误。
这说明针对这些训练记录优化出来的图，能够有效影响MCQ选择。它是逐条优化目标的结果，
共享Writer能否生成同样可读的图、能否写入未训练偏好，仍待学生侧评测。
教师图直接使用MCQ监督，因此不能根据其高于全文对照的分数声称视觉记忆普遍优于文本。

4条T1教师失败记录完整保留：
`entertain_shows:0007`、`lifestyle_fit:0006`、
`professional_work_location_style:0010`、`pet_ownership:0004`。

## 2. 原64教师：同图3+2读回

| 问法 | 对应图 | 错配图 | 灰图 | 全文 |
|---|---:|---:|---:|---:|
| T1 | 64/64 | 32/64 | 27/64 | 57/64 |
| T2 | 64/64 | 28/64 | 24/64 | 59/64 |
| T3 | 64/64 | 36/64 | 31/64 | 59/64 |
| O1 | 63/64 | 32/64 | 29/64 | 58/64 |
| O2 | 64/64 | 29/64 | 28/64 | 61/64 |

同一PNG上O1/O2均正确为63/64。图像hash在问法间一致。
这是原64目标的问法迁移，不能外推成全部730的OOD成绩，也不代表新的语义用途。
O1/O2家族历史上已被观察，沿用“本轮留出问法”的表述。

## 3. C64固定训练源图：一步保持不是全面失败

使用原B write2048生成的固定seed0源图，每条源图做两次噪声不同的原生Writer更新。
两个链的prefix00是同一张输入PNG；prefix01均重新生成，不复制输入绕过Writer。
两噪声是64偏好的重复测量，不能当作128条独立偏好。

| 端点 | 对应图 | 错配图 |
|---|---:|---:|
| 输入源图 | 122/128 | 60/128 |
| 一次无关交换后的生成图 | 109/128 | 60/128 |

输出两噪声均正确为53/64。初写正确样本上的条件保持为109/122；主分母仍是128，
没有删除初写失败项。一步输出相对错配为49次修复、0次退步。

此前首例的一步失败不能代表全部64条。完整证据表明C已有受限的一步保持能力，
但仍存在输入正确、更新后错误的情况。是否改用输入重建目标能减轻这种损失，需I64配对结果。

## 4. C64重新初写的真实递归链：收益在5/10轮显著衰减

本矩阵复用原C已完成的128条真实RGB链，新补齐prefix01读取与统一对照；
不是本次重新训练C。每条链的初写由C模型从灰图生成，其后递归读取上一张实际PNG。
固定训练源图探针与这里的初写输入来源不同，不能混为一条实验。

| 无关交换次数 | 对应图 | 错配图 | 对应图净增正确数 | 两噪声均正确的偏好 |
|---|---:|---:|---:|---:|
| 0 | 122/128 | 60/128 | +62 | 59/64 |
| 1 | 103/128 | 57/128 | +46 | 50/64 |
| 5 | 56/128 | 50/128 | +6 | 27/64 |
| 10 | 55/128 | 51/128 | +4 | 27/64 |

同一偏好的两噪声在0/1/5/10四个被读取端点全部正确：26/64。
该指标没有读取全部11个中间端点，不能写作“每一步都正确”。
条件保持：prefix01为103/122、prefix05为56/122、prefix10为55/122。
全文对照分别为57/64、52/64、52/64、52/64；灰图27/64。

这支持“连续更新后的可读性衰减”这一定位，尚不能单凭准确率区分累积误差、递归输入
分布变化或生成器对source的利用不足。C/I配对会提供下一步证据；不能由C一组结果断言
规范教师目标就是唯一原因，也不能把接近错配的后期分数当作稳定L2成功。

## 5. 当前进程与下一步

- I64已完成2048步，实际训练约54.2分钟，checkpoint SHA256已重算核对为
  `436d1033e3a21a420126b13176a4b007f5e7b80b60ca47202bb6bbb1fd944e4d`，正在执行固定源图探针。
- 17:53现场B730到1807/23360步，原固定预算继续；没有因教师或C成绩选择/更改训练终点。
- 18:01复查B730到2119步，第2048步权重已落盘；I64一步探针已生成74/128条，读取尚未完成。
- C的90开发链、64其余问法和选项位置矩阵仍在执行，本报告没有用部分行给出完整指标。
- 保持原定动作：补全I的一步配对、实际连续链；B730完成后检验未训练dev90的匹配图收益。
  C730/I730仍未启动，也没有增加任何A训练或Judge预算。

## 6. 可复核证据

四个完整矩阵共6056条MCQ原始输出，全部重新调用归档的官方解析函数核验；
对应预定样本、噪声、端点、问法和图像对照，完整键集合无缺行、无重复。
完整原始输出、I64训练轨迹/manifest、C端点hash和现场收据已归档，271个源文件hash验证通过。
C/I训练的父模型、教师目标、实际输入PNG hash、随机种子和2048步预算再次核对一致。
分析程序对缺失读取行及跨问法替换PNG的反例均拒绝汇总，避免改变分母或混入不同图。

目录：[evidence/b-mcq-milestone-20260925-1752](evidence/b-mcq-milestone-20260925-1752)。
压缩归档SHA256：`6cf55547006702006c6fb0f31a0901b818b64956e588f32a707867970fbdb0f8`。
四份 `*-summary.json` 给出固定分母、配对修复/退步、同图OOD、两噪声联合与条件保持。
分析脚本：`scripts/reporting/summarize_prefeval_b_mcq.py`。

源文件参考目录是vendor文件树，不包含独立上游Git元数据；收据中的
`reference_container_git_commit`指所在实验仓库，不能冒充PrefEval上游commit。
实际MCQ源码已随证据归档并hash绑定，实验的数据上游版本继续遵循既有固定清单。
