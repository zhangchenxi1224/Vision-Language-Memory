# I64 pilot 连续链完成，提前并行读取 T1

2026-09-25 23:14 CST。I64 pilot64 的两噪声连续链已全部生成：128/128 条，
每条初写及10次实际Writer更新，共1408张PNG。原推理子进程72800退出码为0，
因原调度shell等待dev归并而暂未回收（Z态）；没有推理任务仍占该卡。

在共享盘逐一重算1408张实际PNG的SHA256，核对全部递归source/output绑定、
64条偏好覆盖、两噪声与28步CFG1 manifest，并重算I64终点权重SHA256，全部通过。
权重为 `436d1033e3a21a420126b13176a4b007f5e7b80b60ca47202bb6bbb1fd944e4d`。
这标志着链生成完成，尚不是长期保持正确率结果。

## 读取调度

Reader队列314433已在原读取实例GPU1执行I64 pilot的T2/T3/O1/O2；23:14读取3704/5376行。
原I64驱动的T1会等dev三分区归并后才开始。为提前获得完整同图3+2矩阵，23:11:04在
`prefeval-k1-h200x2-20260924`已空出的GPU1独立执行原登记T1命令，仍用dc8731c冻结代码、
同一128条PNG链、原输出 `identity64/readback-pilot-T1`、相同0/1/5/10端点及四种对照。
监督进程2469806，实际Reader子进程2469808；23:14已读取334/1344行。原B730继续GPU0。

为避免dev恰好完成时旧驱动同时写入T1，监督进程仅暂置CPU协调器1843603为SIGSTOP，
原调度shell59880继续原来的等待状态。T1执行结束（成功或失败）后，监督进程的finally
自动SIGCONT协调器并写 `identity64/pilot-T1-early-exit.json`。协调器仍等dev三分区齐全后
才释放原shell；原Reader读取器按记录键跳过已完成T1，不增加推理样本或训练预算。

若监督进程被平台杀死：先核对T1实际子进程是否仍活、原输出与GPU，再恢复协调器1843603；
不要直接恢复旧shell59880，否则会与dev分区重复生成。若整个旧实例消失，按现有dev归并
恢复流程处理。启动收据、原命令、冻结commit与现场进程记录已归档。

## 同时推进的节点

23:14共享输出：B730 13173/23360步（56.4%）；I64 dev三分区各38/60，合计114/180链；
C64 dev97/180链。三台实例的实际命令、cwd及GPU占用已核对。四卡实例GPU3在快照完成后空闲。
本次未启动新训练分支、A或付费Judge；源银行及C/I730仍等待B730固定23360终点。

I64 pilot完整3+2和位置矩阵仍在读取，不能用部分样本宣布I长期保持胜过C。
最近一次完整成绩与机制分析仍见 [22:30结果报告](B_MCQ_RESULTS_20260925_2230.md)。

生成证据见 [归档目录](evidence/i64-pilot-complete-20260925-2315)：原始链完成收据、
全部writes记录、manifest、实际PNG核验和调度收据共262文件。PNG及权重保留在原共享盘路径；
压缩包仅收录上述记录，不含尚未完成的读回矩阵。
压缩包SHA256：`2b703e61bfddde418e54a302eac1bf41dee52ee4a851f75a495eac1b218dc651`。
