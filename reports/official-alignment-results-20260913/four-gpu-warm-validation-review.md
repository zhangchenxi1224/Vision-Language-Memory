# Four-GPU warm endpoint: independent functional failure

The fixed additional2880-update endpoint passed development900/900, but its preregistered fresh confirmation did not pass. The complete single-write test scored340/360, with68/72 images passing allfive query forms. The complete RGB-chain test scored410/480, with82/96 writes passing allfive and9/16 fully correct six-write chains. All generated cells and negative controls were retained. These measurements use different noise and new expressions from the preceding0f40767 tests; their aggregate scores are not a controlled before/after comparison.

The complete single-write failure is one expression: `Store ambient as the music preference for the indigo desk train 001123, replacing any earlier choice.` Allfour noise samples fail allfive queries. Outputs include `no active preference`, `no music`, `no music preference`, and one unrelated value. The other68 generated images pass. This cannot be attributed solely to long-chain error accumulation: the failure occurs from the gray source.

The RGB chains preserve all240 reads using the original training expressions. New expressions account for all70 errors. Their contributions are5 errors on an initial ambient write,5 on a subsequent ambient write, and60 on no-op steps expected to preserve ambient or jazz. Both new no-op formulations exhibit failures; in particular the second returns `no active preference` for all40 reads that should preserve ambient/jazz. Source trajectories and all96 PNG/PT/noise records were actually checked by the remote tensor verifier. The evidence supports prioritizing condition-text coverage; it is not proof that one specific encoder feature caused the failures.

The exported package was then loaded by the independent CLI for the fixed first six-write/thirty-read chain. All PNG bytes, raw text, token IDs and termination fields matched the reference exactly. `parity_pass=true` coexists with `reference_functional_pass=false`. Exporting does not turn this endpoint into a usable model.

Both complete archives were downloaded and locally reverified against their remote hashes, all72/96 PNGs,390/480 raw records, original preregistered plan bytes and parent endpoint text. The large checkpoint/PT tensors remain remote and their local omissions are explicit. Confirmation archive SHA256 is `3a238aed2a9162f46e3ab2557eed0d1cabbdf3fa54a91433bc263b28c61d4203`; chain archive SHA256 is `b2757bfc77f4cc3e639b5fed67d8048174e9ea397c77d69ae139605a4329906b`. Package manifest SHA256 is `d1536d754dc3770f3e715b1c0bfaa3278ff61e68b7bad70dce385b3b38f8a2d7`; actual parity-result SHA256 is `f3c0e76a6360c6f0d46d088d75599b5b44af252274c7b75ff115a7357256275f`.

The next Writer fit should combine the broader16-question targets with more equivalent set/clear/no-op expressions across the original state transitions. Retain the45 original conditions. If these now-observed failed expressions enter training, label them as development/regression data and register new expressions for the next independent validation before optimization. Simply repeating the original45 expressions or adding16 unrelated prefixes would not directly address the measured condition-text failure. No augmented Writer fit has been launched yet.
