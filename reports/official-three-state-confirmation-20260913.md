# Three-state shared Writer confirmation

Registered before viewing the endpoint of c90896c-three-state-full1536-20260913. This is one semantic question with ambient, jazz, and no-active-preference states. The previously successful single-state Writer failed interventions; successful target oracles do not establish this shared Writer's behavior.

Use only the fixed 1536-update full-U-Net endpoint from commit c90896c55c7b6cd6f489ccb7848ade2cff587daa, bank SHA5165c059a0a4c0760cd4b0df1663c642132e52bf55a13031cd99207fd200bb86. Require completed result/checkpoint/phase hashes and every state × eight development noises × five questions to pass strict answer and immediate EOS. Missing, duplicate, failed, or overgenerated cells prevent confirmation dispatch. No best checkpoint or guidance selection.

Script scripts/probes/official_three_state_confirmation.py fixes 16 new paired noise seeds for each of the three original training events. Two previously unseen event phrasings per state use the first four of these same seeds. One rewording retains the task prefix; the other omits it. Exact wording and seeds are stored in official-three-state-confirmation-plan-20260913.json before endpoint evaluation. This gives72 generated images and360 raw question answers. Blank/donor controls are scored for each state,30 additional rows; the underlying controls are repeated observations, not30 independent images. Use the existing five Reader questions unchanged, including the two questions withheld from oracle optimization.

Native official Base28-step inference, CFG7.5/imageCFG1, pure Gaussian noise, original gray source, fixed frozen parent weights, FP32 VAE and bf16 Reader. Keep all raw tokens, EOS, latents, complete trajectories, PNGs, model/source/checkpoint bindings and failed answers. All models remain frozen; no optimizer updates or answer rendering into images. Success is per-state/per-wording/per-question, with no compensation from the other states. This evaluates event-conditioned writing from the fixed gray source. It does not establish sequential editing of existing memories, unseen entities, new answer categories, or general memory utility.

The namespace official-three-state-confirmation-noise-v1 is disjoint from all14000 previously used training noises, eight development noises, four prompt controls, and16 earlier single-state confirmation noises. Paired noise reuse across states and wordings is intentional. Deadline remains06:15 before the single-H200 instance expires around06:40.

Metadata audit: the sealed state bank inherited descriptive planned_count/successful_run_count=96 from its ambient parent. There is actually exactly one teacher in each group. These fields are never consulted by training or evaluation; the running bank is immutable. The exporter is now corrected for future banks, while the original sealed evidence is preserved. All current reports use the actual teacher list and per-state draw counts.
