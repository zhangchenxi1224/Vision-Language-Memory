# 追加利用空闲四卡实例：I64 dev 三卡并行

2026-09-25，用户明确提供 `prefeval-k1-h200x4-high-20260924`。实时查询为RUNNING，
四张H200全部0%利用率、0 MiB显存，未检测到实验进程。相同NGC25.02基础镜像。

## 分配

- GPU0/1/2：I64 dev90分别取原记录顺序的0::3、1::3、2::3，各30偏好、两噪声、
  0至10轮真实递归，共60条完整链/卡，三卡合计原登记180条链。
- GPU3：原登记B730第2048步固定快照pilot64的单写及T1，21:52:30已经启动，
  冻结0874d12，父PID395819。与dev快照分离，输出仍为原驱动目的地。

## 协议与互斥

`prefeval_i64_dev_parallel.py`只安排任务；模型生成直接调用dc8731c冻结代码的
configure_strict_cuda_determinism、load_pipe、rollout函数。按偏好分区，不拆开递归链。
逐样本seed依赖pair_id、chain和position，不依赖分区或GPU编号。
原DreamLite/FM、Reader、官方数据、28步CFG1、PNG保存再编码路径均不改变。

旧I64调度shell在其pilot推理子进程继续运行期间，被协调进程暂置SIGSTOP，防止随后
重复启动未开始的dev。没有停止pilot子进程或训练。三个dev分区输出独立目录；
协调进程验证90偏好×2链×11张PNG、checkpoint、逐图hash和递归source/output绑定后，
用目录链接将完整结果原子发布到原identity64/dev，再SIGCONT恢复旧调度shell。
目录链接仅组织已经经Writer生成的完整链，不代替任何生成步骤。

错配图与所有评分仍在完整90条dev池上执行，不在30条分区上计分。不删错误偏好，
不增加训练预算，不提前运行180留出主题。B730、C64、I64 pilot原推理继续原位执行。

如果分区失败，不自动解除旧shell的等待，否则会重复生成；应核对所有分区进程及锁，
用相同代码恢复失败分区，完成后归并。SIGSTOP仅作用于旧调度shell，不能对整个进程组发信号。
若旧实例被抢占，完成分区归并后可直接恢复原identity64驱动，已完成链会校验后跳过。

三卡可将dev链生成这一节点的耗时降到单卡约三分之一；不代表全部实验总时长缩短三倍。
C730/I730与2920源图银行仍等待B730的固定23360终点，不使用中途权重替代。

脚本契约检查3项通过，覆盖分区完整不重复、全链hash验证、损坏PNG拒绝和错误父模型拒绝。

## 部署后现场

调度代码冻结为 `08b1b30b2fb0e906b88f0daf6f6682f1188afaaf`，远端目录
`repos/prefeval-b-parallel-08b1b30`。科学生成实现仍是原dc8731c。
21:57:32在原两卡实例启动协调进程1843603；核实旧shell59880进入T态，同时pilot子进程
72800仍为R态且GPU1为100%负载，B730 GPU0同样持续100%负载。
21:58:12在四卡实例启动三个dev分区，GPU0/1/2的PID分别435293、435296、435297。

22:00:36四卡均100%瞬时GPU负载、各约14.1 GiB显存；三个dev分区各完成首条11张PNG
完整链，合计3/180。三个manifest的checkpoint SHA均为
`436d1033e3a21a420126b13176a4b007f5e7b80b60ca47202bb6bbb1fd944e4d`，
28步/CFG1/两噪声/10轮协议完全相同。GPU3快照pilot完成44/128初写；原I64 pilot为104/128。
原始现场、启动命令、冻结commit和协调收据见
[runtime.json](evidence/high4-20260925-2201/runtime.json)。

自动跟进已登记三实例八卡布局及两个等待状态的恢复条件：旧I64 shell等待dev归并，
旧Reader队列等待snapshot-dev结束。不得误将它们当成故障而重复启动。
原约6小时的I64 dev单卡生成，按历史吞吐预计三卡约2小时，之后还需统一读取；
该预估不含抢占或共享盘拥塞。没有新增训练或更改固定预算。
