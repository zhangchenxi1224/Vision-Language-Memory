# Fresh-noise and event-sensitivity confirmation

Registered before this confirmation is dispatched. Fix the completed full-U-Net512 checkpoint with parent result SHA256 `51332a99686e4de865bdfc344e68f371b132e0d9a741c1f0410c5852563f485b`. It passed all40 original benchmark outputs with immediate EOS. Its same-entity response to a changed event is not yet known. The first successful configuration remains native28, CFG7.5/imageCFG1; do not choose a configuration using the later CFG/conditioning controls.

Use seed namespace `official-full-confirmation-noise-v1`, master20260913, indices0–15. Assert disjointness from all14,000 LoRA3500 training noises, the repeated8-noise benchmark and the four prior zero-update prompt-control noises. The exact plan is stored alongside this note in `official-writer-confirmation-plan-20260913.json` and in the probe identity.

- Original ambient event:16 fresh noises, five unchanged Reader prompts,80 outputs.
- Same event/subject/slot with value changed to jazz: first4 of these noises,20 outputs, gold jazz.
- Same subject/slot with a clear instruction: the same first4 noises,20 outputs, dataset gold `no active preference`. Report raw answers as well as exact scoring so synonymous empty-state responses remain visible.
- Blank and different-answer donor: five outputs each,10 total. Do not duplicate controls over noise seeds.

Total24 generated images and130 raw greedy32-token Reader records; record exact answer plus immediately following EOS, CE diagnostics, output tokens, PNGs and latent trajectories. The raw event goes only to the Writer's existing condition interface; no target image, answer label outside the event, Reader question, lookup bank output, or external text renderer is supplied to generation. Gold strings are used only to score the frozen Reader.

All model parameters freeze after loading the parent checkpoint. Require parent result/checkpoint hash and manifest/cursor equality, original model/condition identity, no parameter/gradient mutation, and final snapshot/hash verification. Original full512 uses the parent single-H200 placement. No optimization, checkpoint selection, resampling after failure, or score-based exclusions. The probe has an explicit deadline before the current notebook expires.

Sixteen successful original-event seeds would strengthen noise robustness for that one event. The jazz/clear interventions test conditional sensitivity; a model that continues to say ambient does not pass them. Failure there requires multiple event/state targets and shared Writer training, rather than relabeling single-teacher memorization as a general memory writer. Even all interventions passing would still require a bounded multi-event evaluation before a usable-version claim.
