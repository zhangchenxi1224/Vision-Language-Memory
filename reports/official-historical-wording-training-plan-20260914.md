# 历史训练表述覆盖对照（启动前固定）

原生条件训练03f8467的完整历史改写仍64条失败；冻结bb权重的raw条件完整对照也仍56条失败，两个版本的单次及RGB连续链均已通过。音乐操作有9种训练表达，16个历史前缀各只有一种。下一轮检验历史表达覆盖，不把条件一致性当作已解决所有失败的原因。

使用与03完全相同的原d1536d7参数初始化、新AdamW、4832更新、全局batch4、seed20260915及全部19328个source/teacher/noise/sigma抽样；31逻辑条件权重不变。训练仍使用native Base完整三分支编码的条件行；推理继续直接走官方28步、CFG1、纯Gaussian、FP32、逐次真实RGB及官方VAE。仍明确区别于官方LoRA示例的raw训练条件与LoRA可训练参数选择。

唯一干预：每个历史前缀新增8个训练表述，加原始表述共9个。按31次draw一个逻辑循环、每9次历史访问均衡遍历9个表达；音乐抽样不变，历史也不因表达变多获得更多权重。151条bank记录、teacher及全部原始开发条件保持字节不变；改写仅选择训练时的条件编码，独立封存每个event/embedding/mask哈希并记录每个实际draw的选择。

新增表达由原事件流严格解析的entity/topic/operation/value生成，保留所有历史事件及顺序。query、gold、teacher latent不进入条件文本；已观察的两种历史验证表达不加入训练。代码和测试拒绝漏掉历史条件、修改原始前缀、重复表达或直接使用现有验证文本。

启动前再次测量全部302张baseline与3020条raw，要求与03初始化的原生latent/RGB/29步轨迹及原始Reader输出逐位一致。新增训练编码独立记录，不改变原始baseline的运行信息。仍要求实际全U-Net串行/四卡梯度预检与各rank参数一致。

固定4832终点后运行全部原1510开发、360单次、480链、960历史前缀及独立CLI六写三十读；保留所有失败。它们是已观察案例的回归与配对诊断，不能声明新测试集、未见实体或多事实能力。即使全部通过，交付前仍须独立的未参与训练表述验收，不能靠已观察案例重新定义可用。

使用当前新四H200，必须等5ef8aa8完整suite结束且GPU实际空闲。旧实例与证据保留。新训练使用单独干净commit/目录；目前本文和单元测试是准备工作，不是已开始训练或得到效果的证据。
