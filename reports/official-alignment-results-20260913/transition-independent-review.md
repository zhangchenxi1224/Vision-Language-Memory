# 2880 步端点的独立诊断与四卡后续实验

当前端点仍未通过功能验收。独立灰图初始写入全部正确，但真实 RGB 连续写入仍有清除失败。

| 已完成的评估 | 严格回答并立即 EOS | 整张图五问法通过 | 完整链通过 |
|---|---:|---:|---:|
| 开发矩阵，45 条件 × 4 噪声 | 880/900 | 176/180 | 不适用 |
| 独立灰图初始写入 | 360/360 | 72/72 | 不适用 |
| 实际 RGB 六次写入链 | 430/480 | 86/96 | 11/16 |

独立单图另有30条空白/异答案对照，全部不通过，未计入360条matched。链上16次初始写入全部正确；五次清除未删除旧状态，其中四次保留jazz，一次保留ambient，紧随其后的no-op也保留实际错误状态，共50条错误回答。不能从灰图初始清除正确推断已有记忆的清除可靠。

验证身份仍明确为 `diagnostic_after_development_failure`。原开发失败没有被独立单图通过或打包成功覆盖。当前参数包在第一条预注册链的六写30读上实际执行了独立CLI，PNG字节及原始回答/token/EOS均一致；`reference_functional_pass=false`。这只证明导出与调用实现一致。

单图归档65,696,825字节，SHA256 `018647bac1e55fd98a5153e86b20ef09c30a3c88aaebd4a8aac6bba898ff7bb6`；链归档87,279,711字节，SHA256 `c7bbda0f57418f10f1a0ef861460f7c4dc896c27e8e709dde82d991883829376`。两者已完整下载，并在本地重新核验全部72/96 PNG、390/480原始回答、原始计划字节及父端点文本。大型PT和checkpoint保留共享盘，远端有完整文件核验和96次真实RGB张量/轨迹审计；本地缺失项在各自local-verification中明确列出。下载中断后使用已验证前缀和剩余字节拼接，最终全文件SHA与远端一致。

用户提供四H200实例后，旧单卡warm-start在baseline阶段安全暂停：optimizer_steps=0、checkpoint=null。保留旧输出，使用相同已封存父参数包、新seed20260914、原45条件/三教师、fresh AdamW以及固定额外2880更新，在新独立运行目录重新开始。

四卡训练代码为 `046c1f1d1c398dbd08578d7c4ba6814343fea0d5`。每卡一个microbatch，每次更新的全局批量仍为4，损失除4后对梯度求和，再裁剪和AdamW。真实全U-Net预检的每个draw和loss均与串行一致，梯度相对L2误差 `3.233411062128955e-08`、最大绝对误差 `3.725290298461914e-09`；初始化与首步更新后四rank参数哈希逐位一致。NCCL归约顺序可能造成FP32舍入变化，没有声称与串行优化轨迹逐位相同。

新噪声下完整warm-start baseline为845/900、169/180图五问法全对；它与上一轮880/900使用不同评估噪声，不是一次参数退化结论。10:16附近实际记录326/2880更新、264.47秒，即约0.81秒/步；旧单卡约1.92秒/步。尚无新训练最终功能结果。后续固定端点收集、原先预注册的新表达/噪声验证、RGB链及独立包回放已由独立套件等待派发，不以训练loss代替验收。

历史16题的直接oracle兼容回读另已完成：FP32解码和RGB uint8各312/320正确，57/64成员在两形式下五问法全通过，80个空白对照全部失败。64份latent和PT/PNG量化关系、720条raw均已远端实际复核。这不是多问题共享Writer结果，也未用此回读替换当前训练教师。
