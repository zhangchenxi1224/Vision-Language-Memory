# Broader oracle compatibility before shared-Writer expansion

The completed historical audit already verified the full16-question campaign. It is not rerun or relabelled as new model inference. Its answer-plus-EOS arm passed64/64 original questions,64/64 first rewrites and61/64 second rewrites, using BF16 VAE decoding. The current Writer uses FP32 VAE decoding and persists quantized RGB images, so those historical outcomes alone cannot qualify its targets.

The new CPU preparation in commitacc0b68 verified the original panel/audit hashes and every one of the64 arm-B endpoint checkpoint, inventory, manifest and index bindings. It sealed the [readback panel](official-alignment-results-20260913/historical-fp32-readback-panel.json), SHA17f00e924ea8a2e66999fd63fb45db8848be8f1c91083155b4a21f9d011b45b5, before any new readback. Local bytes match the remote file. All16 targets and all4 original seeds remain, including previously failed rewrite samples. There is no success-based replacement or new optimization.

Planned readback uses the same verified VAE/Reader model contents, FP32 VAE and bf16 frozen Reader. For each of64 original latent endpoints it checks both the direct FP32-decoded image and its RGB-uint8 quantization, with five questions:the three historical strings unchanged plus two newly fixed generic question formulations. This gives640 matched raw answers. A single RGB1024×1024 gray128 blank is repeated for16×5 questions, giving80 controls. These are16 independent semantic questions with repeated seeds, images and prompts, not720 independent tasks. No best checkpoint or shorter decoding policy is introduced.

The preparation also preserves each target's event stream for future Writer work. Both event and mixed records contribute their exact event text; attached queries, answer choices and scoring metadata are stripped. Two tests cover preservation of all entities/original questions and mixed clear/update events without question leakage. They passed. The historical runtime manifest confirms the same Qwen Reader revision ebb281ec70b05090aa6165b016eac8ec08e71b17 and the verified Mobile VAE snapshot; new execution must still reverify these bindings and the Base/Mobile VAE equality.

**Current status:prepared panel only.** No new FP32/RGB Reader outputs exist, no teacher bank has been qualified from it, and no shared Writer has learned these16 questions. Implement and execute the full readback on available resources after the ongoing fixed transition experiment; preserve all failures. Broader Writer training and functional validation remain separate required evidence.
