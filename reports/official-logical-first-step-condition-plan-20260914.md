# 当前端点的首步条件对照

bb34092的清除失败在原生轨迹第一步已经偏离预期目标，但全部训练日志的清除组在线FM误差没有明显升高。两类观测的模型时刻、噪声与条件不同，不能直接视为矛盾，也不能由平均loss推断推理正确。早期三状态模型的raw/native对照均通过，不能据此排除当前151条件模型的包装敏感性。

固定当前最终checkpoint（SHA737735c4d7d3483b38be2f88d8c48fbe3050b40c8f90c0d49b21e30336455616）、全部151组与各两个已封存开发噪声，执行302个cell的零更新诊断，每个cell三臂：

1. 原生Base条件、CFG1与sigma1的实际首步速度及Euler更新；必须逐位复现已保存的第一步状态，否则终止并保留失败。
2. 原生三分支与CFG1算法相同，但条件采用该组训练时实际缓存的raw event embeddings和mask；记录首步速度与更新。
3. 同一噪声/教师/源图，按官方FM构造sigma0.999的真实带噪训练样本，使用整数timestep999和raw条件，记录训练预测速度。

各臂报告相对于同一`noise-target`标签的完整速度MSE，保存原始速度与对应首步状态。第一、二臂隔离原生提示包装；第二、三臂还同时改变初态微量target混合、timestep和执行batch形状，因此不能把差异单独归因于整数化。sigma0.999是明确选定的边界诊断点，不是替换训练采样分布。

全部302个cell都保留，没有只选10张失败图，也没有模型更新、Reader新评分、推理策略替换或成功认证。Reader与所有模型冻结，source/model/condition/checkpoint绑定逐项检查。完整独立验证及CLI结束后，才允许使用空闲owned单H200运行；不得与当前serial suite争用GPU，不得修改其16bc3d0源码。诊断进程有显式截止时间，输出目录禁止覆盖。

在线训练loss的完整分组描述由`diagnose_training_loss_by_condition.py`对两轮已验证archive重算，每轮4832步、19328draw，四个时间区间和四个训练阶段全部记录。它不是固定模型上的额外FM测试，也没有证明梯度冲突或样本权重不足。新首步对照用于缩小下一轮改动范围，不能提前认定任何一臂更好。
