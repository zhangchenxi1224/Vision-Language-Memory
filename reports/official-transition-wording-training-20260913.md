# Source-transition and wording training, registered before dispatch

The CFG1 three-state candidate has completed its fixed fresh confirmation:320/360 strict answer-plus-immediate-EOS rows, with all240 original-event rows and all80 ambient/jazz rewording rows correct, but0/40 clear rewording rows correct. Its chained validation is still running; already completed first two six-event chains fail retention immediately after their first no-op. Preserve the entire fixed chain run and its failures. These observations motivate training conditional source transitions and multiple event expressions; they do not justify silently increasing the old single-template budget.

Use only the previously verified15-group source bank SHA3d89168c9d36e6df5ded067e54d13128dda1a3cf2c5516600901b017ebe377f2. Its three source PNGs were VAE-decoded from original oracle targets, quantized to RGB, read back15/15 by the frozen Reader, and re-encoded exactly by the actual official runtime. Sources and targets stay unchanged. Add the two fixed event wordings per operation in scripts/experiments/augment_official_transition_bank.py to each original transition. This creates45 conditional groups, with three distinct target tensors and one semantic question. All three source states receive identical text for a given no-op wording, so its correct target cannot be inferred from event text alone. No training input includes the query or a separate answer field.

The old two clear confirmation rewrites are not inserted into training, but their results have been observed and hence they are development diagnostics for this iteration. Do not call a later success on those strings an untouched confirmation. Freeze separate new wording/chain/noise tests before loading new final weights. An output from a previous failed chain is not an oracle source and must never be silently reset to the intended answer during evaluation.

Initialize a fresh sealed official Base U-Net, not the preceding candidate checkpoint. Train all389,968,388 U-Net parameters, keeping VAE/text encoder/Reader frozen. Retain the validated FP32 Writer/bf16 Reader, official target/noise FM over fullsigma with floor(1000sigma), raw event plus image conditioning, pure-noise initialization, and official native28-step inference with explicit textCFG1/imageCFG1.

Fixed budget:2880 optimizer updates, accumulation4, seed20260913, AdamW lr5e-5, betas(.9,.999), epsilon1e-8, weight_decay1e-4, clip1. Exactly11520 draws give256 exposures per conditional group,3840 per target state. This changes training coverage and total exposure relative to the prior three-group run; it is a functional iteration, not a single-variable causal comparison. Checkpoint every16 updates plus first/final/graceful stop, preserving optimizer/RNG for exact recovery.

Evaluate all45 groups at untrained baseline and the fixed final endpoint with four paired development noises and allfive Reader question variants:180 images/900 matched raw rows per phase, plus450 repeated control scores. Four is an explicit new evaluation budget, not a claim that the previous eight-seed endpoint passed. Require every generated image to pass allfive variants for full development success; retain per-source/operation/wording cells and raw token/EOS scoring. No best-checkpoint selection. Further untouched noise and RGB chains must be evaluated before a usable-version claim; this remains a one-entity diagnostic rather than broad memory generalization.

Dispatch on a newly created one-H200 NGC25.02 instance with the verified CUDA12.8 environment and explicit Writer/Reader colocation. Use a four-hour platform lease and a worker deadline at least20 minutes before expiry; the estimated paired evaluation plus training fits this lease. Record exact code/bank hashes and instance/deadline in the dispatch record. Do not disturb the existing candidate chain worker.

Before this training starts, the separate new validation plan is now fixed in [official-transition-validation-plan-20260913.json](official-transition-validation-plan-20260913.json), generated by scripts/probes/transition_validation_plan.py. None of its eight new set/clear/no-op expressions appears in the45-group training bank or the old confirmation prompts. Single writes use16 fresh paired noises per original event and four per each of two new expressions:72 images/360 matched answers. RGB chains use16 six-event sequences/96writes/480answers:for each of four state orders, two repetitions use original trained expressions and two use the new held-out expressions. The generated RGB memory must still be carried between all writes, including after failures. The single-write and chain noise namespaces are distinct from all prior evaluation and the11520 training draws. If development does not pass, any later diagnostic execution must remain labelled as such rather than overriding its failure.
