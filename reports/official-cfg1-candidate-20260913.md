# Native CFG1 candidate after the controlled three-state result

The fixed1536-step endpoint, result SHA4dfb56f942d7dea62ac1d52ec91a0a814df30d4f6accc1238f9310dd822dff14 and checkpoint SHA3c4b0679f16dd7714a662cfaddcd7716f7d3d43a49920898ab19a522d77af38d, failed nativeCFG7.5:20/120 strict answer+EOS cells, and0/24 question/noise pairs passing all five prompts. Its1536 updates/6144 draws are complete, with exactly2048 draws per state. Do not relabel that primary experiment as successful.

Both fixed, zero-update controls are now complete and independently hash-verified: nativeCFG1 and cached-training-condition CFG1 each pass120/120 cells and24/24 five-prompt/noise groups. In both arms, ambient/jazz/no-active-preference each has40 exact raw answers with immediate EOS. On these samples, changing guidance alone is sufficient to restore functionality. The native prompt need not be replaced to obtain this result.

At imageCFG1, the official three-branch expression reduces to w*v_text+(1-w)*v_image. At w=1 it uses the conditional field directly. Full-U-Net FM trained only the conditional field; it did not train an unconditional/image-only dropout branch on these oracle targets. This explains why guidance extrapolation is a plausible mechanism for the measured difference; the controlled result establishes the tested intervention, not a universal theorem about all failures.

The next candidate keeps the fixed same weights, original official native conditioning and28-step scheduler, with explicit textCFG1/imageCFG1. There is no new optimizer update. The global upstream-compatible default remains7.5; new experiment CLI option --base-guidance-scale1 records the choice in dispatch and runtime/checkpoint bindings and affects only Base inference. Training target/noise FM, time sampling, source handling, and optimization settings remain unchanged.

Fresh confirmation is a new registered stage, not a rerun of the failed primary gate. Require the exact successful nativeCFG1 control complete/phase/checkpoint identities and every raw development cell before dispatch. Use namespace official-three-state-confirmation-noise-v2-cfg1, disjoint from the old v1 plan and all prior training/evaluation noises. Keep the existing72-image/360-answer plan:16 paired fresh noises per trained event, two fixed unseen event wordings per state with four paired noises each, plus30 repeated control scores. Save a new fixed plan JSON before running.

Chained validation retains all96 writes and480 answers of the previously registered16 six-event chains, but explicitly uses nativeCFG1 and the new namespace official-rgb-memory-chain-noise-v2-cfg1. It remains disjoint from both confirmation namespaces and the original chain plan. Persist only actual generated uint8 RGB memory between events, continue after wrong answers, and require every question at every step to pass. Do not swap in an oracle state or relax scoring. Register the exact CFG1 chain plan JSON before execution.

Neither fresh confirmation nor chained validation has run at this registration. Passing development controls alone does not complete the goal or prove a usable general memory updater. The15-group source-transition bank remains available if the learned model fails retention or changes under existing-memory source images; no transition training has been launched yet.
