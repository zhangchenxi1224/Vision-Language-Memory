# B MCQ 空闲实例并行恢复

2026-09-25 21:41 CST。用户要求利用已有空闲实例分担任务。本次仅调整资源调度，
训练预算、模型、固定终点、数据与读取协议均不变。

## 已执行

实时核对 `prefeval-k1-read-h200x2-20260924` 两张H200均为0%负载、1 MiB显存，
除Jupyter外无实验进程。原C/Reader所在四卡实例已停止，两个共享文件锁可取得。
原torch环境为2.7.0a0+ecf3bae40a.nv25.02 / CUDA12.8，实际可见两卡。

| 实例 | GPU | 任务与冻结代码 | 当前进程 |
|---|---:|---|---|
| prefeval-k1-h200x2-20260924 | 0 | B730 write23360，5d1045f，继续原运行 | 父59868，训练59899 |
| prefeval-k1-h200x2-20260924 | 1 | I64完整递归链，dc8731c，继续原运行 | 父59880，rollout72800 |
| prefeval-k1-read-h200x2-20260924 | 0 | C64剩余dev链及T1，dc8731c，21:34:20恢复 | 父313512，当前rollout336084 |
| prefeval-k1-read-h200x2-20260924 | 1 | 固定2048快照dev早读，随后继续Reader队列 | 早读父346391；Reader314433 |

GPU1于21:34:33先恢复原Reader队列（5d1045f），补完C pilot第10轮选项位置矩阵，
随后等待C dev、I pilot/dev完整端点。确认Reader没有子进程、队列处于等待后，
21:38:57将其暂置SIGSTOP，利用空窗运行已登记的snapshot-dev（0874d12）。
早读父shell的EXIT trap自动SIGCONT恢复原Reader；没有中断任何正在读取的矩阵。
这是等待队列调度，不是实验冻结，也不能把Reader的T态误判成故障。

snapshot-dev使用同一个B730第2048步固定权重、V1、dev90两噪声单写和T1四种对照；
输出与原B730驱动后续读取的路径相同。0874d12的scripts/experiments与5d1045f无差异。
不新增训练、不用早读结果选点，不影响23360固定终点。snapshot-pilot尚未另行启动。

## 实际推进证据

21:41:06快照：新增实例GPU0为100%负载，GPU1为38%瞬时负载，均约14.1 GiB显存；
C dev完整链由48增加到49/180；I pilot为94/128；固定2048 dev初写完成10/180。
C pilot第10轮换位已有complete收据，本次没有把它重新汇总成能力结论。
完整运行收据、父进程cwd/命令、启动日志路径和队列状态见
[runtime.json](evidence/parallel-20260925-2141/runtime.json)。

已停止本任务此前重复排队的 `prefeval-b-read-h200x2-20260925` 请求；该实例仍为PENDING
且未运行实验时执行停止。无需新申请资源即可把原两卡工作扩为两实例四卡并行。
禁止实例 `dl-clear-retain-h200x4-20260914` 未使用；其他实验未停止或迁移。

## 后续调度

自动跟进已更新为当前布局。早读父进程结束后自动恢复Reader；若父进程已死而Reader
仍为T态，须核对GPU空闲、命令和cwd后CONT原PID。若平台再次抢占整个容器，按原冻结
脚本和已有完整输出恢复；不得根据旧PID重复启动。B730与I64保持原位。

本次并行减少C链与读取等待GPU的时间，不会让单卡B730优化本身加速，也不保证总耗时
按GPU数线性缩短。C730/I730仍需等待同一个B730终点和共同2920张源图银行。
