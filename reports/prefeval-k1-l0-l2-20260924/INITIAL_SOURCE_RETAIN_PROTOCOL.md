# 下一轮：仅用学生初写图学习一步保持

2026-09-24，启动前登记。此前完整 write-only 64 条链在第 5/10 轮已失去匹配图收益；
固定首例在 retain2048 后仍第一次更新失败，但其 B 图经原生 VAE 往返仍可正确读取。
一种待检验解释是：把已难以读取偏好的深层学生前缀用于恢复教师目标，增加了条件映射难度。
这不是“前缀 latent 已无信息”的证明；原完整保持结果仍待生成。

本轮保持 A/B、固定 64 偏好、各自 write2048 父权重、同一教师目标、同一 2,048 步预算、
每步 batch4、初写/保持各 4,096 次抽取、相同数据次序/噪声/sigma/优化器均不变。
唯一改变：保持抽取的输入图统一取该偏好的学生 `seed-0/prefix-00.png`，
而不取第 1—10 轮对应的深层前缀。仍轮换原位置的真实干扰交换，不把偏好文字补入输入。
全部 64 条都保留，不按照 Reader 成败挑选样本。该图经保存/reopen/VAE encode 后使用，
不能直接使用教师 latent、教师图或缓存文本历史作为旧记忆。

训练仍执行已有官方 FM 方程、原条件路径、原 target bank。
先检验从可用初始状态学习一步保持是否改善；这不等于已学会多步递归。
固定最终步数后从灰图重新生成每组 64 条、两噪声、0—10 轮完整链。
优先读取 T1 的 0/1/5/10，匹配和错配均报告，随后执行相同 90 条内部开发集。
已训练与未训练偏好分别列出；同一冻结 PNG 自然回答与 MCQ 分开统计。
不使用 OOD 来选择检查点、步数或本轮参数，不提前读取最终 180 条测试表现。

与原保持组比较需要明确：本轮改变了保持输入分布；即使改善，也只能支持这种输入课程有效，
不能仅凭改善断言先前失败只由信息丢失造成。初写性能退化同样报告。
本轮训练使用现有本任务 Reader 实例空余显存，不触碰禁用实例，不修改任何正在运行的代码工作树。


## 实际启动

北京时间 08:47，在 `prefeval-k1-read-h200x2-20260924` 的 GPU 0/1 启动 A/B，
冻结执行提交 `8cd5785`，独立工作树 `repos/prefeval-k1-initial-retain-8cd5785`。
父启动进程为 3370993/3370997，训练进程 3371031/3371033。
启动前两卡空闲分别为 133,722 / 143,167 MiB，未申请额外实例。
两组各 64 张源图均已核对与原 write2048 初写图片逐字节哈希相同，父权重实际哈希一致。
完整参数与源图列表见[启动记录](evidence/initial-source-retain-launch.json)。
训练完成后自动执行新的 pilot/dev RGB 链，优先 MCQ，再补自然回答；尚未产生本轮效果结论。


## 已核对前 32 步

北京时间 08:55，A/B 分别已记录 45/135 步。两组前 32 步尚未发生源图位置变化，
与原保持训练的 128 次逐样本损失及 32 个梯度范数全部逐值相等。
截至观察时的全部抽样偏好、干扰位置与 sigma 也与原组一致，损失/梯度均有限且梯度非零。
新 manifest 除源码与源图模式外多记录了 `split=pilot`、`snapshot_steps=[]`；
原版本未记录这两个字段，实际数据和快照设置没有改变。
这项核验确认干预按预期执行，不代表后续学习成功。
详见[实际轨迹对照](evidence/initial-source-retain-first32-verification.json)。

## 首例提前读取

在本轮训练尚未完成时登记：沿用 `entertain_games:0001` 与同主题错配 `entertain_games:0007`，
待新模型真实生成两条链及错配链后，先读取 T1 MCQ 的 0/1/5/10，各组 16 条。
问题、选项排列、Reader 和 32-token 预算不变；所有图必须绑定本轮最终 2,048 步权重。
这只是让已登记指标更早返回，不代替完整 64 条分母、不改变训练或挑选检查点。
主评测仍从同一批冻结 PNG 继续，不把这 16 条额外计入整体分母。

首例读取等待进程 A 3553609/B 3553610 已在现有 Reader 实例启动，并通过实际进程查询确认存活；
[参数与预期分母](evidence/initial-source-retain-first-case-launch.json)。
