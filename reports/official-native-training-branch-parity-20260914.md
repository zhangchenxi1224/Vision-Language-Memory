# 新训练条件与官方原生分支的数值核验

03f8467四卡对照已通过完整初态门控并开始更新。为检查新训练条件确实对应官方Base推理条件，在保留且实际空闲的旧单H200上运行冻结bb34092终点的零更新诊断，不接触正在训练的权重或实验配置。

全部151条件、两个既定噪声共302格原样保留。新条件helper的embedding哈希必须逐位对应当前03训练runtime；再次实际调用官方原生编码器时，其第三行embedding和mask必须逐位等于新缓存。观察官方UNet第三行输入，要求其实际noise/source拼接与训练路径一致；真实native首步速度和Euler更新还必须逐位复现17f35be已有记录。

比较官方UNet条件分支、官方CFG1之后的速度，以及新训练单行UNet在同一个sigma1/时间1000上的输出。额外比较整数1000与浮点1000，以区分时间类型与batch数的数值影响。sigma1是边界诊断，不冒充torch.rand抽到的训练样本；不会扩大实际训练时间范围。预先固定相对L2和相对最大误差阈值均为2e-6，全部302格都完成并保留超阈值格，不因部分数值差异而只保留成功格。

输出实际速度PT、条件/掩码哈希和逐格数值误差。全部模型冻结，0优化、0新Reader评分；此检查只证实输入和数值接口的一致性，不能证明完整生成、读取或多事实记忆可用。当前四卡训练和固定全功能验证继续独立运行。

## 完整结果：输入一致，速度严格阈值296/302通过

105f521已完成全部302格；独立CPU检查新旧604份PT，并重算速度误差、真实source/noise以及Euler更新，本地便携记录亦全部重算通过。全部条件embedding/mask与官方实际编码第三行一致，源图/噪声输入一致；全部native速度与首步更新复现原17f35be记录。整数1000与浮点1000的速度差值在全部302格为0。

| 比较 | 全302格最大相对L2 | 最大相对最大值误差 |
|---|---:|---:|
| 新训练单行 vs native条件分支 | 1.592519e-6 | 3.368092e-6 |
| 新训练单行 vs native CFG1后速度 | 1.592526e-6 | 3.368092e-6 |
| 整数1000 vs 浮点1000 | 0 | 0 |

预设的两种相对误差都不超过2e-6这一门槛为296/302通过；6格超出最大值误差阈值，不能记为全通过。它们是jazz清除原wording0的seed1、gray清除wording8的seed0、ambient清除wording4的seed0、ambient清除wording8的两个seed、jazz清除wording8的seed0。阈值未放宽，全部原始速度保留。输入语义一致已有实测，单行与三行前向仍存在这些微小数值差异；不能据此推断所有时间点或完整功能都等价，也不能将这些差异直接认定为旧功能失败的原因。

完整CPU证据archive SHA **cd9796d7a0e650b148afa3b08abf3758c8c8e3c9dff487dedfbdfae10e7eb279**，157,908bytes；CPU summary **27114ec2e85cdb61c9ead43220209aaddc1ca798d187260231eaf49a8d301907**；probe complete **0f6db794a2ea8ae0d5249ae335340d5234100794b943b64ec98a3b43416a5795**。三项便携证据在official-alignment-results-20260913下，PT留在共享盘。另已预注册并启动[完整raw条件读取对照](official-broader-raw-condition-control-20260914.md)，直接检验完整28步后的1510条matched答案，而非仅凭首步误差选择方案。
