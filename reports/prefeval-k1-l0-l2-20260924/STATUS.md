# 部署与首轮状态

2026-09-24，实验仍在运行，不是最终性能报告。

- 临时实例：`vlm-unet-diag-h200x2-20260909`，两张 H200，使用前已核实无计算进程、
  GPU 均为 0% / 1 MiB；镜像和软件版本与原实验相同。该实例约在 03:21 自动停止。
  目标图和额外 T1 MCQ 诊断均已完成，相关进程正常结束；后续任务全部在专属实例运行。
- 后续专属实例：`prefeval-k1-h200x2-run-20260924`，两张 H200，普通优先级，RUNNING，
  未设置短时自动停止，当前两张卡分别仅运行 A/B Writer。
- Reader 专属实例：`prefeval-k1-read-h200x2-20260924`，两张 H200，普通优先级，RUNNING，
  未设置短时自动停止。排队结束后核实两张空卡与相同运行库，已接续 A/B 读回。
- 禁止使用用户已另作分配的 `dl-clear-retain-h200x4-20260914`，未在其上启动任务。
- 分支：`codex/prefeval-k1-l0-l2-20260924`。
- 教师执行提交为 `5153444`，全部四分片已正常结束。
- 后续执行提交：`1c4aa2a`，独立工作树 `repos/prefeval-k1-stages-1c4aa2a`。
  调度入口 `c7b2356` 仅允许两卡映射和指定冻结代码路径，不修改训练实现。
  初始两卡映射为 GPU 0 执行 A、GPU 1 执行 B；现将 Reader 迁往第二个独立两卡实例。
  原调度等待进程 5697 已停止，Writer 独立会话 82195/82203 保持运行，未重新启动训练。
- 所有产物根：`/inspire/ssd/project/exploration-topic/czxs26210936/runs/prefeval-k1-l0-l2-20260924`。
- 教师已完整完成 A 64/64、B 64/64；逐图核实 288 个不重复的有效更新、
  T1/T2/T3 各 96 次、B 每个问法×正确位置各 24 次，PNG/latent 哈希一致。
  两组各 18,432 个有效更新，合计 36,864，未因续跑增加正式步数。
- 首段 FM 两组均已完成 2,048/2,048 步，原 Python PID A 82267/B 82241 正常结束并进入生成。
  已核实各 8,192 个样本、64 偏好各出现 128 次，最终检查点实际哈希正确，
  详见 [首段训练报告](WRITE_STAGE_TRAINING.md)。
  write-pilot 原生生成已全部完成：A/B 各 128/128 张 PNG；write-dev 也均完成 180/180 张 PNG。
  已自动转入保持训练所需的 64 条真实 RGB 前缀生成（每条 11 张图）；
  本次观察 A/B 各完成 26/64 条链，保持 FM 尚未开始。
  固定首例独立读回已完成：B 匹配图两条噪声链 2/2、错配图 0/2；A 两种条件均 0/2。
  自由回答均截断，补充 judge 的首例结果也未显示学生遵循成功；
  详见 [首例学生诊断](FIRST_STUDENT_DIAGNOSTIC.md) 与 [补充评分](FIRST_FREE_SUPPLEMENTAL.md)。
  Reader 实例上 A 教师 Python PID 56011 仍运行，已生成 1,393/2,560 行；
  B 教师 Python PID 56007 已正常结束，1,280/1,280 行生成完成。
  O1/O2 生成记录保持封存，不用于下一轮参数或检查点选择。
- 完整学生读回执行提交仍为 `82c2780`，各自在同卡教师读回结束后运行。
  B 已自动接续至学生 write-pilot T1 自然回答与 MCQ，Python PID 1326389，启动 shell 55918；
  A 启动 shell 55905 仍等待教师读取。独立的全量 pilot T1 MCQ 已完成，成绩如下。
  顺序为单写 T1、最终 0/5/10 T1、L1 覆盖 T1、最终其余四问法；
  使用已经冻结的 Writer 端点，O1/O2 不参与下一轮参数选择。
- 新增 `run_prefeval_k1_student_mcq.sh`：等待固定 2,048 步 Writer 的 pilot/dev 两噪声链
  PNG 全部完成后，独立执行 T1 MCQ，不等待完整自然回答矩阵。它复用同一评测实现，
  pilot 使用已经完成的教师侧灰图/全文基线，dev 由 A 生成共享基线；输出单独保存在
  `student-T1-mcq`，不与完整评测重复计入分母。训练预算、教师选择与 OOD 使用规则不变。
  该入口原主实例 PID A 592907/B 592910 已随迁移停止，Reader 实例新 PID 为 55909/55927；
  执行提交仍为 `6561833`。全量 pilot T1 MCQ 已完成：A 匹配 64/128、错配 63/128；
  B 匹配 123/128、错配 61/128。两链均正确分别为 A 30/64、B 60/64。
  无解析失败或截断，见 [单写 pilot 结果](WRITE_PILOT_T1_RESULTS.md)。dev 的图片已齐备，
  MCQ 读取 Python PID A 829752/B 829749 已启动；本轮固定 90 条偏好、两条噪声链，
  A 同时生成共享空图/全文参照。两组均已完整结束：A 匹配 81/180、错配 80/180；
  B 两者均为 80/180；共享空图 42/90、全文 77/90。新内容写入尚未显示匹配图片收益，
  见 [开发集单写结果](WRITE_DEV_T1_RESULTS.md)。原读取 Python 829752/829749 正常完成。
  write-dev 原 Python PID A 1266930/B 1266396 已正常接续至 training-prefixes，
  新 Python PID A 1556969/B 1554828，两个原 Writer 启动会话 82195/82203 持续运行。
  保持训练前的 T1 MCQ 配对参照也已登记并部署：等待进程 A 892117/B 892118，
  读取实现仍为 6561833、调度脚本 e89c746；等待 64 条前缀齐备，不占用 Writer GPU。
  已对固定首例单独完成 0/5/10 与逐步 0—10 诊断：B 初写正确，第一次干扰后即错误，
  A 起点已错；两组错配也均错。无截断/解析失败。该单例不代替全量保持成绩，
  详见 [首次干扰诊断](FIRST_PRERETAIN_DIAGNOSTIC.md)。
  固定首例的四位置探针也已完成：B 两链各 4/4（共 8/8），A 共 0/8；
  只针对这一条训练偏好，执行提交 `2bcd0f1`，不当作全部偏好的位置鲁棒性成绩。
  完整学生读回等待进程也已迁移，新 PID A 55905/B 55918，执行提交仍为 `82c2780`。
- 迁移前 A/B 教师读回各完成 390/370 条，逐行 JSON 和键唯一性有效，原始文件哈希保持不变；
  无须截断或丢弃记录。新实例已追加至 394/374 条，证明实际恢复推进。
  旧 MCQ 等待进程的 exit-status 已归档至远端迁移目录，不能视为当前任务完成。
  运行库、旧进程和新进程证据见 evidence/reader-migration；原始文件备份保留在远端。
- 扩容所需 730 条完整五问法已准备：666 条新请求改写，加原样保留的 64 条 pilot。
  覆盖和留出隔离检查通过，相关 7 项测试通过；准备完成后已启动下述独立 730 条实验。
- 另查到官方跨主题划分中的文本重合：完整 730 train 与 180 official 有 3 条相同偏好、
  相同归一化原题；64 pilot 与 dev/official 均无重复，730 train 与 90 dev 无重复。
  不改官方划分，主指标仍为完整 180；后续扩大训练后须披露三条文本暴露。
  详见 [登记中的暴露说明](PLAN.md) 与 evidence/preference-text-overlap.json。
- 第二轮覆盖实验已登记并准备部署，详见 [730 条方案](SCALE730_PLAN.md)。
  代码固定于 `331092d2533c3b7cfb562fd15e69012c84c80f44`，远端独立工作树
  `repos/prefeval-k1-scale730-331092d` 已就绪且干净；相关数据与汇总测试 9 项通过。
  新运行根 `runs/prefeval-k1-scale730-20260924` 下，两组各 64 条旧目标已逐一验证哈希和
  T1/T2/T3 一致性并建立复用链接；新增优化仅处理其余 666 条。
  新实例 `prefeval-k1-730-h2-20260924` 排队结束，现为 RUNNING，节点 qb-prod-gpu2177。
  排队时平台曾报告已用 16/17 GPU，申请 2 GPU 超额；期间未改动其他实验资源。
  已核实两张 H200、空闲计算进程和相同运行库后启动；主进程 27368，
  A 两分片 27390/27391 使用 GPU 0，B 两分片 27392/27393 使用 GPU 1。
  首次轨迹观察确认 A 137、B 135 次新增更新，全部 CE/梯度有限且梯度非零，
  四分片均覆盖登记中的新偏好；这只是训练已执行证据，不是语义能力成绩。
  运行库、冻结提交、分片任务与轨迹见 evidence/scale730-launch-observation.json。
  首轮冻结目录和现有训练、读取进程均保持原样。

技术验证已完成：A/B 各 12 次 latent 更新、PNG 保存/回读；各两次实际 FM 更新；
各自原生 28 步生成、PNG 落盘后再输入第二次写入。技术输出放在 smoke / writer-smoke，
正式训练不沿用其权重。首个失败仅因启动环境变量不匹配，保留在 smoke-env-failed-61ce035。

完整训练记录见 evidence/teacher-training-summary.json。最后 12 次前向 CE 的逐状态中位数为
A 0.220278、B 0.000004360；范围分别为 0.005486–1.592300 和 0.000000068–0.000017348。
两组目标与答案长度不同，这些数字不表示记忆准确率。A 的训练误差较分散，
后续需结合原题读回判断是否是目标图阶段的瓶颈；不按损失筛掉教师。
原始首例 PNG 与优化轨迹见 FIRST_TARGET_DIAGNOSTIC.md。

完整 64 条原题 MCQ 诊断已完成：A 匹配图 43/64、错配图 36/64；
B 匹配图 64/64、错配图 32/64；共享灰图 27/64、全文参照 57/64。
无解析失败或截断。它们是固定教师端点结果，不能替代 Writer 或递归保持成绩。
已定位自然回答与 MCQ 唯一标签不完全一致、发布回答含格式瑕疵的具体案例，
官方 gold 与训练目标均保持原样。详见 [原题 MCQ 报告](TEACHER_T1_MCQ_RESULTS.md)。

北京时间 02:12:40 平台因 preempt 驱逐实例，随后自动从 qb-prod-gpu655 调度到
qb-prod-gpu418。已通过平台 events、原 PID 消失和 GPU 空闲共同确认进程退出；
从共享盘恢复四个未完成状态的 latent + Adam，恢复步分别为 A 168/72、B 96/96。
保留中断前完整 trace，并将未提交的 51 个步骤单独留档后从检查点重算，
不增加最终 288 个接受更新步数。原训练提交和所有数据不变。
恢复记录见 evidence/preemption-1-recovery.json；新四分片 PID 为 28713/28714/28715/28716，
接续进程 PID 为 29239。恢复后已核实实际更新继续推进至后续样本，不仅检查进程存活。

北京时间 02:36:24 再次发生平台抢占，原专属四卡实例进入 PENDING，已停止该申请。
第二次恢复前完成 A 47/64、B 48/64；按最近检查点恢复，34 个未提交计算步骤留档并重算。
记录见 evidence/preemption-2-recovery.json。首次尝试在 CPU 实例读取 checkpoint 因其基础镜像
没有 PyTorch 而在导入时退出，未改动训练文件；随后在相同 GPU 镜像上完成恢复。
曾申请的普通四卡 `prefeval-k1-h200x4-high-20260924` 被项目总配额 108/108 阻止，
小规格低优先级 `prefeval-k1-h200x2-20260924` 也在等待节点资源，两者现已停止，未运行训练。
先借助已核实空闲的现有两卡实例恢复目标图，随后普通优先级专属两卡实例于 02:48:59 就绪。
现有实例只完成已在运行的目标图优化，后续阶段在新的专属实例接续，避免主动中断训练。

## 首个固定教师端点的 T1 读回（单例诊断）

样本 `entertain_games:0001`：官方偏好是 `I don't enjoy games with pixel art graphics.`

| 输入 | 官方 MCQ 正确项 D（Stray） | 原始自由回答观察 |
|---|---|---|
| A 教师 PNG | 错，C（Celeste） | 开头明确提到不要 pixel art |
| B 教师 PNG | 对，D（Stray） | 输出通用游戏推荐，未在开头明确此偏好 |
| 灰图 | 错，C | 通用推荐 |
| 完整文本 | 对，D | 提到 pixel art 限制 |
| 错配 A 教师图 | 错，B | 换成错误图所属的避免 horror 偏好 |
| 错配 B 教师图 | 错，C | 通用推荐 |

原始记录见 evidence/first-frozen-teacher-{A,B}-T1.jsonl，实际 PNG hash、输出 token、
选项排列和解析结果均保留。此表仅用于验证读回与干预流程；没有官方自由回答 judge 分数，
不是总体准确率、不是未训练偏好泛化，也不证明 L0–L2 已完成。

第一性原理解释：可读出训练回答相关信息与支持另一种输出任务是两回事。
A/B 原题格式对比需要完整分母；B 本例正确位置 D 还恰好等于最后训练更新的正确位置，
因此补充预先固定的 T1 四位置读回诊断，检查是内容读取还是字母位置依赖。
这个诊断不改训练预算、不使用 OOD、不挑教师，也不替代原始官方 MCQ 指标。

四位置诊断已完成：同一冻结 A PNG 为 0/4，同一冻结 B PNG 为 4/4；
B 的输出随正确位置分别为 A/B/C/D，排除了该例“只输出最后训练字母 D”的解释。
它仍不能排除对这组特定选项内容的适配，也不能据此宣称获得可迁移的偏好表征。
证据见 evidence/first-teacher-{A,B}-four-positions.jsonl。

自然回答训练输入采用官方 benchmark 的 `Please respond within 300 words` 后缀，
目标仍是官方发布的完整 SFT 回答；这是本实验的 Reader 任务适配，不声称逐 token 复现
原 Mistral SFT 输入。MCQ 采用官方原提示，Qwen 生成完整 XML，预算 32 token。

## 尚待完成

完整教师与 Writer 的统一读回；真实 RGB 前缀生成及第二段保持 FM；训练侧与开发侧两噪声链 0/5/10 轮保持；
L1 单有效偏好的覆盖探针；根据内部开发 T1 定位瓶颈后的修正与扩容；冻结后的 180 官方评估。
官方 Claude 自由回答评分凭据仍未配置；替代 judge 补充评分已运行。
不把没有执行的阶段或缺少 judge 的指标写成成功。

后续检查确认现有 DashScope 凭据可用，已按单独协议启用固定 Qwen3-Max 快照的补充评分；
此前 gateway 的 401 不代表所有服务都不可用。首例 14 条自然回答的 56 次调用已完成，
原始提示、响应与聚合全部保存，见 [首例补充评分](FIRST_FREE_SUPPLEMENTAL.md)。
该 judge 出现可核实的事实错误，结果必须标注替代 judge 与局限，不能当作官方 Claude 分数。
官方 Claude 凭据仍未配置，完整自然回答评测仍未完成。

完整 B 教师的 T1 自然回答 128 条和当时已生成的 A 132/256 条已导出不可变快照，
见 evidence/teacher-T1-free-snapshot-001。仅导出 T1，不涉及 OOD 分数。
补充 judge 正在按 B 后 A 顺序处理，复用输入完全一致的 6 条已评分记录；
中途遇到 HTTP 429 后，增加请求间隔和有限传输重试，未改变提示、模型、生成预算或评分规则。
未完成分母保留为缺失，不依据当前局部评分宣布 A/B 总体自然回答成绩。

对该不可变 T1 快照追加了不依赖 judge 的输出对照：B 全部 64 对匹配/错配图的
自然回答均不完全相同，共同起始 token 数中位数为 6；匹配 63/64、错配 64/64 回答截断。
这说明图片变化影响了生成，但不证明变化携带正确偏好。A 快照只有 33/64 条偏好，
同样没有完全相同的匹配/错配回答；匹配截断 12/33、错配 24/33，不能与完整 B 直接作总体比较。
原有 300-token 上限及生成记录保持不变；每题结果和源文件哈希见
[自然回答图像对照](evidence/teacher-T1-free-controls-001.json)。
