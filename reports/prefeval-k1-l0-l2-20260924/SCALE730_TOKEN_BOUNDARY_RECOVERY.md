# 730 条目标图训练：官方答案换行边界的恢复

2026-09-24。A-0 分片在完成 20 条新增偏好后，于 `entertain_games:0050` 第一次前向退出。
其他三个分片持续运行。故障不是损失发散，也不是偏好内容不可学习，而是原 Reader 适配器
要求“拼上答案后，聊天模板前缀的 token 不变”，这条发布答案恰好违反了该假设。

官方答案以 `\n Of course!` 开头；生成提示以换行 token `198` 结束。
把两段文本整体分词会把两个换行合并为 token `271`，改变已经送入模型的生成前缀。
扫描全部 730 条 × T1/T2/T3，共 2,190 个输入，仅该偏好的三个问法存在此问题。
其余 2,187 个输入均保持前缀，联合后缀与单独答案分词一致；未删掉或改写任何官方答案。

## 修复与验证

执行提交 `eba0166ebaa61faadb2daa62622415abca12bb33` 增加显式恢复模式：
普通输入仍使用原联合分词；只有会改变前缀的输入，才保持实际生成提示 token，
追加能精确解码回原答案字节的续写 token。保留答案首换行与一个真实 EOS，
正确延长 attention mask 与多模态 token 类型；损失仍是答案 token 的平均 CE。
这没有改动 PrefEval 数据、官方答案、DreamLite FM 或生成预算。

验证包括 26 项 Reader/选择评分/缩放相关测试，以及相同 H200 运行库的实际 VAE→Reader 前后向：
出错样本三种问法均得到有限损失、非零 latent 梯度，197 个答案 token（含 EOS），
原答案可精确解码，生成前缀末尾保持一致。
另一个普通样本的恢复模式开/关，损失、latent 梯度和 target token 逐位相同。
实际模型探针没有执行 optimizer 更新，不混入正式 288 步预算。
首次探针因缺少两个确定性环境变量在模型加载前退出，补齐训练环境后通过，未改模型配置。

## 恢复后的实测状态

在独立冻结工作树 `repos/prefeval-k1-boundary-eba0166` 恢复 A-0，PID `705041`，GPU 0。
既有登记身份继续绑定 `331092d2533c3b7cfb562fd15e69012c84c80f44`，
实际修复代码版本独立写入 `execution` 元数据，没有把新代码冒充旧执行版本。
其余三个进程和其工作树保持原样。

- 故障样本此前接受的更新数为 0；恢复后恰好完成 288 步，T1/T2/T3 各 96 次。
- 288 条轨迹的损失和梯度均有限，梯度非零，最终 PNG/latent 哈希验证通过。
- 恢复前完成的 20 条目标，其 PNG/latent 哈希全部不变，没有重训或丢弃。
- 已继续到下一条 `entertain_shows:0001`，观察时达到第 173 步。

原总入口已经记住旧 A-0 的失败，待原三个分片结束后会以 1 退出。
独立接续进程 `705048` 因此等待四个目标分片全部完成及原入口退出，再启动原冻结
`331092d` 的两组后续阶段；若 Writer 已启动则拒绝重复启动。
教师 T1 MCQ、FM 步数、固定检查点、单写评测均沿用原登记方案。
这是技术故障恢复证据，不是视觉记忆准确率或 730 条实验完成声明。

- [全部训练原题的分词边界扫描](evidence/scale730-token-boundary-probe.json)
- [实际模型前后向验证](evidence/scale730-token-boundary-recovery/real-model-probe.json)
- [恢复后 288 步及旧目标哈希验证](evidence/scale730-token-boundary-recovery/recovery-verification.json)
- [原始失败、恢复入口和训练轨迹](evidence/scale730-token-boundary-recovery)
