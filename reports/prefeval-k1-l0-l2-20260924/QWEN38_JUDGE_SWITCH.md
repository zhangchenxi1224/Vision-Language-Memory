# Judge 切换至 Qwen3.8-Max

2026-09-24，按用户“现在 judge 评测开始了吗，换成 Qwen3.8-max”的明确要求执行。

## 执行范围

旧 judge 已完成教师、已训练学生和 A dev 等评分。切换时旧 dev 缓存共 705 条：
704 条 complete、1 条 pending；其中包括已完成的 A dev360。旧缓存和已发布报告原样保留，
不能将其中的分数改名为新模型成绩。

已停止本任务的旧 B dev 评分进程及其两个本地排队父进程。远端训练、Writer 生成、
Reader 读取均未停止；原用户要求的暂停后续新增训练迭代仍然有效。

新 judge 请求模型为 `qwen3.8-max`，真实通用探针和正式评分响应均返回该模型名。
使用用户指定的模型 ID，不声称它是不可变快照。每次原始响应保留供应商返回的模型信息。
正式评判显式设置 `enable_thinking=false`，保持此前非思考评判方式，temperature=0、
max_tokens=100。四份 PrefEval 原提示、XML parser、官方聚合规则保持不变。

官方说明：[模型](https://help.aliyun.com/zh/model-studio/qwen3-8-max)、
[思考模式](https://help.aliyun.com/zh/model-studio/deep-thinking)。
Qwen3.8 默认开启思考，因此本次显式关闭；通用非思考探针及正式评分均已成功。

## 固定输入重评队列

复用现有 Reader 输出，不重新生成答案、不修改图片、不读取 OOD 成绩。
完整重评保证新模型下 A/B、教师/学生以及匹配/错配/灰图/全文参照可比较，
而不是只把旧评分未完成的尾部换成新 judge。

| 顺序 | 批次 | 回答数 |
|---|---|---:|
| 1 | B 未训练 dev90，匹配/错配、两噪声链 | 360 |
| 2 | dev90 共享灰图/全文参照 | 180 |
| 3 | A 未训练 dev90，匹配/错配、两噪声链 | 360 |
| 4 | A 教师及 pilot 共享灰图/全文参照 | 256 |
| 5 | B 教师匹配/错配 | 128 |
| 6 | A 已训练学生匹配/错配、两噪声链 | 256 |
| 7 | B 已训练学生匹配/错配、两噪声链 | 256 |
| 8 | 官方发布目标一致性诊断，单独标注 | 64 |
| 合计 | 8 批 | 1,860 |

前 7 批包含 1,796 条 Reader 回答，第 8 批不作为视觉记忆成绩。
输入文件、哈希、完整分母与逐条标识见 [清单](evidence/qwen38max-T1-input-manifest.json)。
新原始评分单独写入 `evidence/judge-qwen38max-T1/`。每个完整批次结束后保存独立汇总。
解析失败按原规则保留、单列；不补写、不剔除、不因判定结果重采样。
这些分数仍标注 `substitute_judge`，不是官方 Claude 模型复现。

本地运行器 `.cache/run_qwen38_judge.py` 已以后台进程启动；首条正式记录四项评分完成，
响应模型核验为 `qwen3.8-max`。尚无完整批次成绩。
Judge 单元测试 8 项通过，包括非思考请求参数、原预算及既有重试/解析失败处理。

后续本任务自然回答评分沿用此次 Qwen3.8 设置。已有旧模型报告作为历史结果保留，
在新模型完整重评前不提前改写实验结论。MCQ 不依赖 LLM judge，因此其成绩不因本次切换改变。
