# 固定bb终点的完整raw条件推理对照

17f35be全302格首步诊断中，raw训练条件显著降低了bb终点的速度误差，但首步误差不能证明完整28步后的可读性。补充这一直接功能对照，同时保持03f8467四卡原生条件训练和9e27050完整验证不变。

固定bb34092的4832步最终权重、151个源图/事件条件、原有两个开发噪声、五种问法、FP32 Writer和bf16冻结Reader。使用既有TrainingConditionNativeSampler，将真实raw训练embedding/mask重复至原生三个分支，原生28步、source分支、CFG1/image-CFG1和Euler循环不改。与bb原生推理完整3020条记录逐格比较，其中1510条matched；负对照要求不变。保留所有302张图、29状态轨迹和原始32-token输出，不重评分或挑选噪声。

这是0参数更新、仅推理条件编码变化的配对诊断，明确偏离官方原生diptych包装。即使全部已有开发格通过，也必须继续独立表达、真实RGB链和导出CLI验证，不把它当可用版本。本轮不选择中间checkpoint，也不改变四卡训练的固定终点。

资源使用保留的单H200，在105f521分支数值诊断完成且GPU实际空闲后启动，硬截止09-14 05:40CST，至少保留75分钟才执行，晚于该窗口则停止等待并报告。用户旧r3/r2不触碰。目标是比较两条明确方案：训练改成native条件，或在旧raw训练终点使用一致raw条件推理；不能把两者混报为同一个实验。
