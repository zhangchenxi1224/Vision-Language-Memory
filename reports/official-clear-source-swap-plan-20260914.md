# 清空失败的权重与源图交叉诊断

旧/新完整功能和 PNG 验证均已失败，下一步定位清空首错的来源。本诊断使用已经观察过的原有案例，不作为新测试集或可用性评分，也不进行训练。

固定原有 sequence-0、sequence-1 的四次重复，共八个 block。每个 block 从封存的 step-3 PNG 开始，实际执行 step-4 清空和 step-5 保持不变，每次写入后读取原五种问题。交叉两版权重与两版各自生成的源图：03/03、03/b9、b9/03、b9/b9，共 32 次独立 CLI、64 次写入、320 次读取。覆盖同组成功与失败重复，不按此次对照结果筛选。

使用已经验证过且保持干净的 7b82309 CLI，其 `--initial-image` 接收实际源 PNG。事件、种子与 query 完全来自原完整验证；CLI 只读取事件/种子/问题命令，不接收 gold 或预期输出。每组后续 no-op 沿用本次清空真正生成的 RGB。包加载器继续检查固定 DreamLite、Reader 和参数身份，保持原生 28 步、CFG1、纯 Gaussian 和 FP32。

两个对角组合必须逐字节复现原两步 PNG，并复现全部原始输出/token/EOS 字段；否则先认定重放差异，不能据交叉分数下因果结论。原对角匹配答案预计分别为 80/80 和 60/80，来自既有完整证据；交叉组结果未知。相同权重下换源图、相同源图下换权重，分别提供局部输入依赖与参数变化证据；不能直接推广到新实体或所有表达。

固定[计划](official-alignment-results-20260913/clear-source-swap-plan.json) SHA256 `03121197ae33e7e9a13e96bf0db4f5fd1597947d9efc911beddf7cce50cc7c86`，包含全部输入 PNG、命令、原参考输出和包身份。可携带执行包 SHA256 `6b9eb1206ee3d7f3582c76bba59353e85f2a0d2fc62b89929adc4930cbad4f3e`；编排脚本 SHA256 `455097a73f77461c16b2d07a4494d8fbfff8be5830560533f308813f3b0df4ea`。编排脚本仅调用原 CLI，所有模型代码仍为干净 7b 固定目录。脚本先验证这三个身份以及实际四卡空闲，输出目录不得已存在，操作失败保留现场。

实际已在新4H200启动，driver1560037；09:17观察到4/32个CLI完成、8个已启动，四个CUDA worker实际运行。截止09:45 CST。完整交叉结果仍未知，运行结束后需下载完整输入/输出，重新核对实际 PNG 和原始读取，再决定下一轮训练改动。
