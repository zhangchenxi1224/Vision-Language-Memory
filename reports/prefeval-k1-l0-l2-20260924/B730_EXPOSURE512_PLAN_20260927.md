# B730 固定曝光扩展：23,360 → 93,440

本轮回答：在教师目标、输入版本、官方 FM、模型结构与更新规则均不变时，增加曝光能让 730 条已训练偏好的初写可靠性提高多少。不是 R 保持训练，也不把训练侧改善解释为新偏好泛化。

- 恢复 `prefeval-b-mcq-20260925/robust730/train/resume.pt` 的权重、AdamW、RNG、训练游标；起点 23,360，总终点 93,440，还需 70,080 次更新。
- effective batch=4，lr=5e-5，原教师 730 份 latent、原 V0/V1 文件、原随机种子及按全局 draw 的轮换保持不变。每偏好总曝光从 128 增至 512，V0/V1 从各 64 增至各 256。
- 中间检查点固定 46,720、70,080，最终 93,440。预算和检查点不随 dev 成绩调整。
- GPU0 保留原逐 micro-batch 累积的训练实现；GPU1/2/3 运行冻结检查点评测，不参与模型选择。
- 续训专用入口从原 B730 `5d1045f` 的 Writer 派生，只添加显式预算扩展与恢复入口；完整 FM 更新循环与原文件 AST 一致，相关 conditioning、FM、数据与读回实现均未改变。
- 各检查点：train730 的 V0/V1，各两噪声，T1；pilot64 和 dev90 的 V0/V1，各两噪声，同图 T1/T2/T3/O1/O2。只做现有 MCQ，不扩展自然回答或付费 Judge。
- 控制：匹配、同 topic 错配、灰图、文本。报告全分母准确率、匹配减错配、配对修复/退步、两噪声全对、原 64 子集与旧 23,360 终点的变化。
- 旧 train730/V1 基线为 907/1460，错配 587/1460，两噪声全对 421/730。补齐此前不存在的全量 train730/V0 基线。其他基线复用旧原始读回。
- 先以 full-pool train730 评测；pilot64 的错配供图池不同，独立评测，不从 train730 错配子集冒充 pilot 成绩。
- 训练每 128 步保存完整恢复断点。重启时只重做断点后未提交的尾部，已提交全局 step/draw 不重置。

执行实例：用户指定 `prefeval-k1-l012-h200x4-20260924`，4×H200/80 CPU/900 GiB，NGC 25.02 镜像。2026-09-27 检查时节点 qb-prod-gpu565，低优先级。临时新建的 `b730-512-h200x4-20260927` 已撤销。

输出共享目录：`/inspire/ssd/project/exploration-topic/czxs26210936/runs/prefeval-b730-exposure512-20260927`。原 B730 与正在进行的 C/R 实验目录不写入。
